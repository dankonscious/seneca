# -----------------------------------------------------------------------------
# Workflow: Deploy
# -----------------------------------------------------------------------------
name: Deploy

on:
  push:
    branches:
      - main
      - master

jobs:

  deploy-ftp:
    name: Deploy
    uses: EnclaveLLC/automation-workflow/.github/workflows/deploy.yml@main
    permissions:
      actions: read
      contents: write
      issues: write
      pull-requests: read
      id-token: write
    secrets: inherit

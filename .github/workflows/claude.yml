name: Claude

on:
  workflow_dispatch:

jobs:
  claude:
    uses: dankonscious/automation-workflow/.github/workflows/claude.yml@main

    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}